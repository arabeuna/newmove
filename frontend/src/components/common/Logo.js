import React from 'react';

const Logo = ({ className }) => {
  return (
    <img
      src="/logo192.png"
      alt="Move"
      className={className}
    />
  );
};

export default Logo; 